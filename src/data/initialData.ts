import {
  cleanInitialSchool,
  cleanInitialFiscalYears,
  cleanInitialUsers,
  cleanInitialStudents,
  cleanInitialRevenues,
  cleanInitialBudgetAllocations,
  cleanInitialLearnerActivities,
  cleanInitialProjects,
  cleanInitialTransactions,
  cleanInitialStrategies,
} from '../utils/storage';

export const initialSchool = cleanInitialSchool;
export const initialSchoolData = cleanInitialSchool;

export const initialFiscalYears = cleanInitialFiscalYears;
export const initialUsers = cleanInitialUsers;

export const initialStudents = cleanInitialStudents;
export const initialStudentsData = cleanInitialStudents;

export const initialRevenueItems = cleanInitialRevenues;
export const initialRevenuesData = cleanInitialRevenues;

export const initialBudgetAllocations = cleanInitialBudgetAllocations;
export const initialLearnerActivities = cleanInitialLearnerActivities;

export const initialProjects = cleanInitialProjects;
export const initialProjectsData = cleanInitialProjects;

export const initialTransactions = cleanInitialTransactions;
export const initialStrategies = cleanInitialStrategies;
